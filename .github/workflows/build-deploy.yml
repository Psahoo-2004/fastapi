name: Build and Deploy Code
on: [push,pull_request]

jobs:
    job1:
        runs-on: windows-latest
        steps:
            - name: pulling git repo
              uses: actions/checkout@v4
            - name: say hi to Pratyush
              run: echo "Hello Pratyush"
        
